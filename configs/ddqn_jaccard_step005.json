{
  "reward_mode": "jaccard",
  "step_cost": 0.05,
  "use_action_mask": true,
  "total_steps": 150000,
  "hidden_sizes": [256, 256],
  "lr": 0.001,
  "discount": 0.99,
  "buffer_size": 100000,
  "batch_size": 128,
  "epsilon_start": 1.0,
  "epsilon_end": 0.05,
  "target_update_every": 500,
  "_comment": "Iteration 5: global Jaccard terminal reward. Replaces stochastic p_good/p_bad with clean sparse signal. step_cost=0.05 is starting point for ablation sweep."
}
