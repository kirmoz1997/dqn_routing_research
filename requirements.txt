# ──────────────────────────────────────────────────────────────────────
# Pinned (lock) dependencies for multiagent_dqn_routing
#
# Regenerate after changing pyproject.toml [project.dependencies]:
#   pip install -e '.[dev]' && pip freeze --exclude-editable | sort > requirements.txt
#
# For development install use:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -e '.[dev]'
# ──────────────────────────────────────────────────────────────────────

# Core (runtime)
joblib==1.5.3
numpy==2.4.2
scikit-learn==1.6.1
scipy==1.17.0
torch==2.10.0

# Dev extras (analysis / plotting)
matplotlib==3.10.8
pandas==3.0.0
tqdm==4.67.3
